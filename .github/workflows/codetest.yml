name: Code test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master


jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.7]


    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Build and install MultiNest
        run: |
          DEPS_DIR="${GITHUB_WORKFLOW}/deps"
          mkdir ${DEPS_DIR} && cd ${DEPS_DIR}
          git clone https://github.com/JohannesBuchner/MultiNest.git
          cd ${DEPS_DIR}/MultiNest/build && cmake .. && make && sudo make install

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install cython scipy numpy matplotlib future pytest ipython
      
      - name: Install OMpy
        run: pip install -e .

      - name: List versions
        run: ipython check_env.py

      - name: Test with pytest
        run: pytest ./tests/
